
# Write your MySQL query statement below
SELECT class
FROM Courses
GROUP BY class
HAVING count(DISTINCT student) >= 5;



顺序
FROM -> (ON JOIN)-> WHERE -> GROUP BY ->WITH -> HAVING -> SELECT (DISTINCT) ->ORDER BY

因为grp by之后的条件 所以是having
